# harness build + run artifacts
harness/bin/
harness/.worker.log
harness/.worker.pid
harness/.sample-ids.txt
harness/.run-window

# counter tool clone (fetched on demand by uvx / make)
harness/counter/

# python
.venv/
__pycache__/

# local secrets (Datadog keys etc.) — .env.example is tracked, .env is not
.env
harness/.env

# editors
.idea/
.vscode/

# os
.DS_Store
